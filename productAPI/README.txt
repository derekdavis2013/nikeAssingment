Application can be started with `npm start` and will listen on port 3000.

Assumptions made:
    --Two products may not have the same priority
    --Changing the priority for one product must adjust adjecent priorities
    --No fields may be null. When replacing a product, all fields all required.
